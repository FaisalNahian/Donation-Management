<% content_for :page_title do %>
	<h1>New Page</h1>
<% end %>

<% form_for :page, :url => {:action => :create } do |f| -%>
	<%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f} %>
	<%= f.submit 'Save' %>
<% end %>